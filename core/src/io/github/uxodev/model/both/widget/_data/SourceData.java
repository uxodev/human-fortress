package io.github.uxodev.model.both.widget._data;

import io.github.uxodev.model.both.widget.token.source.Source;
import io.github.uxodev.model.both.widget.token.source.SourceToken;

import java.util.HashMap;

public class SourceData {
    public static final HashMap<String, SourceToken> SOURCE1_TOKENS = new HashMap<>();
    public static final HashMap<String, SourceToken> SOURCE2_TOKENS = new HashMap<>();

    public static final HashMap<String, HashMap<String, Source>> SOURCE_TEMPLATES = new HashMap<>();
}
