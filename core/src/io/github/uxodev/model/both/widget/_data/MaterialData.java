package io.github.uxodev.model.both.widget._data;

import io.github.uxodev.model.both.widget._data.token.PropertyToken;
import io.github.uxodev.model.both.widget.token.material.MaterialToken;

import java.util.HashMap;

public class MaterialData {
    public static final HashMap<String, MaterialToken> MATERIAL_TOKENS = new HashMap<>();
    public static final HashMap<String, PropertyToken> PROPERTY_TOKENS = new HashMap<>();
}
